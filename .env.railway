# Railway Environment Configuration
# This file contains Railway-specific environment variables

# Application Settings
NODE_ENV=production

# Port Configuration
# Railway will automatically set PORT - don't override it
# PORT will be set by Railway (typically 8080 or dynamic)

# Database Configuration (set these in Railway dashboard)
# DATABASE_URL=mysql://user:password@host:port/database

# JWT & Security Configuration (set these in Railway dashboard)
# JWT_SECRET=your-production-jwt-secret-key-min-32-characters-long
# JWT_REFRESH_SECRET=your-production-refresh-secret-key
# COOKIE_SECRET=your-production-cookie-secret
# DEVICE_HMAC_SECRET=your-production-device-secret

# Frontend URL (Railway will set this automatically)
# RAILWAY_STATIC_URL will be available

# CORS Origins (automatically configured for same-origin in production)
# FRONTEND_URL will be derived from RAILWAY_STATIC_URL

# Email Configuration (optional - set in Railway dashboard if needed)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
# EMAIL_FROM="Lucky Voucher System <noreply@yourdomain.com>"

# SMS Configuration (optional - set in Railway dashboard if needed)  
# TWILIO_ACCOUNT_SID=your-twilio-sid
# TWILIO_AUTH_TOKEN=your-twilio-token
# TWILIO_PHONE_NUMBER=your-twilio-phone

# Important Notes:
# 1. Don't set PORT in Railway dashboard - let Railway manage it
# 2. Don't commit sensitive values to git
# 3. Set all secrets in Railway dashboard environment variables
# 4. The app will automatically use Railway's PORT and URL
