{
    "name": "voucher-system-frontend",
    "version": "1.0.0",
    "description": "Voucher System - Frontend Service",
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "start": "cd frontend && npx serve -s build -l $PORT"
    },
    "dependencies": {
        "serve": "^14.2.1"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}